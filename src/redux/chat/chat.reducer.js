const initialState = {
    chatLoading: false,
    friendId: "",
    conversationId: "",
    messages: [],
}

const chatReducer = (state = initialState, action) => {
    switch (action.type) {

    }
}

export default chatReducer