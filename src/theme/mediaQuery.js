const breakPoints = {
    sm: "560px",
    md: "786px",
    lg: "940px"
}

export const device = {
    sm: `(max-width: ${breakPoints.sm})`,
    md: `(max-width: ${breakPoints.md})`,
    lg: `(max-width: ${breakPoints.lg})`
}

