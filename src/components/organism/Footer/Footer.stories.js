import Footer from "./Footer";

export default {
    component: Footer,
    title: "organism/Footer"
}

const Template = args => <Footer {...args}/>

export const DefFooter = Template.bind({});
DefFooter.args = {};